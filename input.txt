/* Declare variables */
integer x;
decimal y;
logical flag;

/* Assign values */
x = 5.2;
y = 3.14 +x/(3-1);
flag = true;

/* Print variables */
print(x);
print(y);
print(flag);

/* Arithmetic and comp_assign */
x += 10;
y *= 2.0;

print(x);
print(y);


integer i;
i from 1 to 5 in (x = i*2) {
    print(x);
}
when {
    is (x > 10) { print("BIG"); }
    otherwise { print("SMALL"); }
}
x=9;

bound (x) {
    in (10, 20) { print("Between 10 and 20"); }
    above (20) { print("greater than 20"); }
    below (10) { print("below 10"); }
    equals (15) { print("Exactly 10"); }
}